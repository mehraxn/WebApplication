This version was generated from the submitted LA Walks project.

Changes made:
- All image files were removed from the ZIP.
- Inline JavaScript was moved out of the HTML templates.
- Reservation page JavaScript is now in static/js/reservation.js.
- Completed tours page JavaScript is now in static/js/completed_tours.js.
- Bootstrap JavaScript remains loaded as an external CDN script in base.html.

Note: because images are intentionally removed, image paths in templates/database may display as missing until images are added back.
